import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    primary: {
      main: '#fff',
      heading: '#1E1B1B',
      paragraph: '#232536',
      text:'#939191'
    },
  },
});

export default theme